{
  "name": "rtl-pro",
  "version": "1.0.0",
  "description": "RTL Pro - Transform your Shopify store to Hebrew RTL with one click",
  "private": true,
  "scripts": {
    "dev": "shopify app dev",
    "build": "npx prisma generate --schema=web/database/schema.prisma && cd frontend && npm install --legacy-peer-deps && npm run build",
    "start": "node web/index.js",
    "setup": "npx prisma generate && npx prisma migrate deploy",
    "info": "shopify app info",
    "deploy": "shopify app deploy",
    "generate": "shopify app generate"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0",
    "@shopify/shopify-api": "^11.0.0",
    "@shopify/shopify-app-express": "^5.0.0",
    "@shopify/shopify-app-session-storage-prisma": "^4.0.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "serve-static": "^1.15.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.3",
    "prisma": "^5.10.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
